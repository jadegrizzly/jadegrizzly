// Meteor.publish('usersData', function() {
//   return Meteor.users.find();
// });

if (Meteor.isServer) {

  // Meteor.publish("usersData", function() {
  //   return Meteor.users.find({});
  // });  

}